# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

本项目是一个**个人主页**（personal site / portfolio website）。根据 `.gitignore` 推断，项目使用 Node.js 生态，具体框架和构建工具尚未确定。

## 当前状态

- 最小化脚手架 — 仅有 `.gitignore`
- `master` 分支上尚无 commit
- 未配置 git remote
- 包管理器：待定（无 `package.json`）

## 常用命令

_项目尚未初始化，暂无构建/测试/开发命令。_
