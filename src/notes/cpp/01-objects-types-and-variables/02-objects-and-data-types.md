---
title: 对象、值与数据类型（Objects, Values, and Data Types）
date: 2026-08-25
---

# 对象、值与数据类型（Objects, Values, and Data Types）

## C++ 语言中的对象（Object）

在系统设计中，人们常把轮胎、发动机这类保存自身状态并承担相应职责的程序部件称为对象。C++ 语言规则中的对象范围更广：即使一份数据只负责保存左前轮当前的气压，它也可以是一个对象。

```cpp
double front_left_pressure{2.5};
```

这行代码使程序中出现一个 `double` 对象，并让名称 `front_left_pressure` 指代它。对象当前保存的值是 `2.5`。

**在当前模型中，C++ 对象是程序运行期间具有类型和生命周期，并占用存储的实体。**生命周期（lifetime）描述对象从开始存在到不再存在的时间范围；类型规定对象能够表示哪些值，以及程序可以对它进行哪些操作。

## 对象与值（Object and Value）

对象与它当前保存的值不是同一个概念：

```cpp
front_left_pressure = 1.75;
```

这次操作把 `front_left_pressure` 的值从 `2.5` 改成了 `1.75`，名称指代的仍然是原来的对象。**对象身份回答“这是哪一个对象”，值回答“这个对象当前保存了什么”。对象可以继续存在，而它的值在允许修改时发生变化。**

两个对象也可以具有相同的类型和值，却仍然彼此独立：

```cpp
double front_left_pressure{2.5};
double rear_left_pressure{2.5};

front_left_pressure = 1.75;
```

最后，`front_left_pressure` 保存 `1.75`，`rear_left_pressure` 仍然保存 `2.5`。相同的数值不会把两个对象合并为同一个对象。

## 对象与存储

程序运行期间，对象在 C++ 抽象机（abstract machine）的存储中存在。这里的存储是语言用来规定对象行为的模型，不是对某块物理硬件位置的直接承诺。

**对象占用存储，不等于源代码中的每个对象都必须永久对应主内存里一块固定可见的区域。**语言首先规定程序应当表现出的结果，具体实现只需符合这些规则。对象与实际存储位置之间更精细的关系，需要在程序开始观察对象地址时才能完整建立。

## 比特与字节（Bits and Bytes）

数字电路会把不同范围的电压或电荷状态归入两个容易区分的逻辑状态，并分别记为 `0` 和 `1`。真实的电气状态可能连续变化，但只区分两种逻辑状态更容易抵抗噪声，也更容易构造稳定的运算与存储电路。这种使用两种状态表示信息的方式称为二进制（binary）。

二进制表示中的每一位称为比特（bit），值只能是 `0` 或 `1`。多个 bits 可以共同构成比特组合（bit pattern），从而区分更多信息。现代通用计算机通常将连续的 8 bits 组织成一个字节（byte），一个 8-bit byte 可以形成 $2^8=256$ 种不同的 bit patterns。

**一个 C++ byte 至少包含 8 bits；C++ 使用 byte 作为衡量对象存储大小的基本单位，但没有规定它在所有实现中都必须恰好包含 8 bits。**8-bit byte 是当前主流通用平台采用的实现。

## 数据类型（Data Type）

整数数量、轮胎气压、发动机状态和文本最终都可以表示成 bits，但相同的 bit pattern 按照不同规则解释，可能具有完全不同的含义。程序还需要知道一组 bits 能够表示哪些值、允许参与哪些操作，以及采用怎样的存储和表示规则。这些规则由数据类型（data type，通常简称 type）建立。

**类型是对象在 C++ 语言模型中的属性，不是普通对象旁边自动保存的一枚运行期标签。**单独取得一段原始 bytes，通常无法只凭这些 bytes 判断它们原本属于 `int`、`double` 还是其他类型；编译器根据源代码中已经建立的类型关系和语言规则，知道应当怎样解释和使用对象。

回到最初的代码：

```cpp
double front_left_pressure{2.5};
```

`double` 是 `front_left_pressure` 的类型，规定它按照浮点类型的规则表示和处理数值。将值从 `2.5` 改为 `1.75` 不会把对象变成其他类型。**对象的值可以发生变化，但对象的类型在其生命周期内保持不变。**

当前主线会使用下面几组常见的 C++ 内建类型：

| 表达的信息 | 常用类型 |
| --- | --- |
| 逻辑状态 | `bool` |
| 整数 | `short`、`int`、`long`、`long long` 及其无符号形式 |
| 字符与编码单元 | `char`、`signed char`、`unsigned char` |
| 浮点数 | `float`、`double`、`long double` |

这些分组只描述本章将要解决的问题，不是 C++ 类型系统的完整分类。

相关语言规则可参阅 C++23 工作草案中的[对象模型](https://timsong-cpp.github.io/cppwp/n4950/intro.object)、[类型](https://timsong-cpp.github.io/cppwp/n4950/basic.types)与[基本类型](https://timsong-cpp.github.io/cppwp/n4950/basic.fundamental)。
