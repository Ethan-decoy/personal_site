---
title: 序列存储与引用有效性（Sequence Storage and Reference Validity）
date: 2026-09-11
order: 4
---

# 序列存储与引用有效性（Sequence Storage and Reference Validity）

采样值可能持续增加。容器能够管理更多元素，但之前取得的元素引用是否还能使用，取决于追加怎样改变元素存储。**容器对象仍然存在，与它原来管理的元素仍然有效，是两项不同的条件。**

## 在末尾追加一个元素

初始化时可以用花括号给出一组采样值；新的采样值在运行过程中到来时，需要向已有序列继续添加元素。

`push_back` 是 `std::vector` 提供的公开成员函数，用来在序列末尾追加一个元素。调用写作 `samples.push_back(value)`，其中 `samples` 是要修改的容器，`value` 表示要追加的数值。

下面是包含 `<vector>` 后可放入函数体的片段：

```cpp
std::vector<double> samples{240.0, 245.0};
samples.push_back(250.0);
```

成功调用后，序列由两个元素变为三个元素，依次保存 `240.0`、`245.0` 和 `250.0`，新元素可以通过 `samples[2]` 访问。`push_back` 的返回类型为 `void`；它直接修改调用它的容器，不能通过 `const` 对象或只读引用调用。

给已有元素赋值只改变该元素的值，元素数量保持不变；追加则建立一个新元素，让 `size()` 增加一。不能通过给越界下标赋值来追加元素。

## 容器对象与连续元素存储

`std::vector<double>` 将元素按照下标顺序连续存放。容器负责取得保存这些元素所需的存储，并在正常销毁时清理元素、释放自己管理的存储；调用者不需要另外逐项执行清理操作。

名称 `samples` 指定容器对象，`samples[0]` 指定它管理的一个 `double` 元素。元素增加时，容器可能更换保存元素的存储区域；即使名称 `samples` 仍指定原来的容器对象，旧元素的引用也可能已经失效。

元素数量（size）与容量（capacity）分别描述两件事：

| 操作 | 返回值的含义 | 能否据此访问元素 |
| --- | --- | --- |
| `samples.size()` | 当前已经存在的元素数量 | 有效下标小于这个数量 |
| `samples.capacity()` | 无需重新分配存储就能容纳的元素总数 | 预留空间不等于已有元素，仍须检查 `size()` |

`capacity()` 与 `size()` 返回相同的数量类型，容量不小于当前元素数量。多出来的容量允许容器继续建立元素，不能当作已经存在的 `double` 使用。

## 追加元素怎样影响引用

若新数量超过原容量，容器必须取得足够的新存储，在其中建立元素，并释放原来的元素存储。这个过程称为重新分配（reallocation）。已经取得的旧元素指针和引用都会失效，不会自动跟随同一下标找到新位置。

若容量足够，末尾追加不会重新分配，指向已有元素的指针和引用仍然有效。这项结论针对已有元素；范围 for 使用的原结束位置仍会因追加而失效，不能由此推导出可以在同一个范围 for 中继续追加。

> [!WARNING]
> 追加成功前若 `size() == capacity()`，这次追加就会触发重新分配。重新分配后再读取此前保存的元素引用，具有未定义行为。需要访问当前元素时，应当从仍然有效的容器重新取得它，不能继续使用旧引用。

容量的具体增长幅度由实现选择。花括号给出三个元素，不保证容量恰好为三；一次追加也不保证容量按某个固定倍数增长。判断引用是否保留，应当依据操作及容量条件。

## 已知数量时预留容量

`samples.reserve(count)` 调用成员函数 `reserve`，请求容器预留至少能容纳 `count` 个元素的容量。成功调用后，容量不小于这个数量，但不会因此增加元素数量；若原容量已经足够，它不重新分配存储。

下面的程序在取得元素引用之前，为三次采样预留空间：

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<double> samples{};
    samples.reserve(3);

    samples.push_back(240.0);
    const double& first{samples[0]};

    samples.push_back(245.0);
    samples.push_back(250.0);

    std::cout << first << '\n';
    std::cout << samples.size() << '\n';
}
```

`reserve(3)` 之后序列仍为空。三次 `push_back` 才依次建立三个元素；它们没有超过已预留的容量，因此引用 `first` 始终指向有效的第一个元素。程序输出 `240` 和 `3`，无需假定容量恰好等于三。

`reserve` 自身也可能重新分配，所以预留容量应当发生在需要保留元素引用之前。它只为这段不超过容量的追加提供条件，不保证任意后续操作都保留元素引用。

> [!PRACTICE]
> 当业务已经知道将保存的大致数量时，可以提前预留容量，减少增长过程中更换存储的工作。不能把 `reserve` 当作每次追加前都必须执行的步骤，也不能在预留空间后直接用下标访问尚未建立的元素。

这些正常路径结论以操作成功为前提。存储或长度请求无法满足时，操作可能通过异常（exception）报告失败：它不会正常返回并继续执行调用点之后的语句，而是转向相应的异常处理；没有处理者时，程序会终止。对这里的 `std::vector<double>`，`push_back` 或 `reserve` 抛出异常时，原容器保持不变。

## 借用不能维持容器的生命周期

正常离开局部容器所在的作用域时，容器会销毁它管理的元素并释放相应存储。元素引用不会因此拥有另一份数据，也不会让这些元素继续存在。

下面是一个独立的错误示例：

```cpp
#include <vector>

const double& first_pressure_kpa() {
    const std::vector<double> samples{240.0, 245.0, 250.0};
    return samples[0];
}
```

返回表达式指定局部容器中的第一个元素。函数结束后容器与元素都已销毁，调用者得到的是悬空引用；通过返回的引用读取采样值具有未定义行为。返回类型中的 `const` 只限制修改权限，不能延长局部容器或元素的生命周期。

如果调用者只需要这个采样数值，可以把返回类型改为 `double`。返回时读取元素值并建立独立结果，局部序列随后销毁，不会破坏这个结果。

> [!IMPORTANT]
> 借用序列中的元素，需要同时满足两个条件：负责管理它的对象仍然有效，相关操作也没有让原元素访问关系失效。复制一个独立的 `double` 值可以脱离这层依赖；保存元素引用则需要继续维护它。

## 参考资料

- [C++23 工作草案：序列容器的末尾追加操作](https://timsong-cpp.github.io/cppwp/n4950/sequence.reqmts#98)
- [C++23 工作草案：vector 的连续存储与自动存储管理](https://timsong-cpp.github.io/cppwp/n4950/vector.overview)
- [C++23 工作草案：容量、预留空间与重新分配](https://timsong-cpp.github.io/cppwp/n4950/vector.capacity)
- [C++23 工作草案：vector 追加操作与失效条件](https://timsong-cpp.github.io/cppwp/n4950/vector.modifiers)
- [C++23 工作草案：容器的销毁与存储释放](https://timsong-cpp.github.io/cppwp/n4950/container.reqmts#25)
